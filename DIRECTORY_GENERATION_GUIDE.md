# DingCoder AI - 目录生成功能指南

## 🎯 功能概述

DingCoder AI 现在支持智能目录选择功能，让你能够精确控制生成的Java文件放置在哪个目录中。

## 📁 目录选择逻辑

插件按以下优先级确定生成目录：

1. **项目视图选中目录** - 最高优先级
2. **用户手动选择目录** - 中等优先级  
3. **默认src/main/java目录** - 最低优先级

## 🚀 使用方法

### 方法1: 使用项目视图选中目录（推荐）

1. 在左侧项目树中，**鼠标点击**选中想要生成文件的目录
2. 点击代码面板中的"✨ 生成Java文件"按钮
3. 系统会提示："检测到选中目录: /path/to/selected，是否在此目录生成文件？"
4. 点击"是"确认，文件将在选中目录生成

### 方法2: 手动选择目录

1. 如果没有选中目录，或选择"否"拒绝使用检测到的目录
2. 系统会弹出目录选择对话框
3. 浏览并选择想要的目标目录
4. 点击"确定"，文件将在选择的目录生成

### 方法3: 使用默认目录

1. 如果取消目录选择，系统会使用默认的`src/main/java`目录
2. 如果该目录不存在，系统会自动创建

## 📦 包目录处理

当生成的Java代码包含`package`声明时：

```java
package com.example.service;

public class UserService {
    // 类内容
}
```

系统会：
1. 检测到包声明：`com.example.service`
2. 询问："检测到包声明: com.example.service，是否要创建对应的包目录结构？"
3. 如果选择"是"，会在目标目录下创建：`com/example/service/`
4. 最终文件路径：`目标目录/com/example/service/UserService.java`

## 🔄 批量生成特殊处理

使用"批量生成"功能时：
- **自动使用**项目视图中选中的目录
- **自动创建**包目录结构（不再询问）
- 所有文件都生成到同一个基础目录

## 💡 实用技巧

### 技巧1: 在特定模块生成
```
my-project/
├── user-service/src/main/java/     ← 选中这里
├── order-service/src/main/java/    
└── common/src/main/java/
```

### 技巧2: 在测试目录生成
```
my-project/
├── src/main/java/
└── src/test/java/                  ← 选中这里生成测试类
```

### 技巧3: 在自定义目录生成
```
my-project/
├── src/main/java/
├── generated/                      ← 选中这里
└── templates/
```

## ⚡ 快速上手

1. **选择目录**：在项目树中点击想要的目录
2. **生成代码**：点击"✨ 生成Java文件" 
3. **确认位置**：确认弹出的目录提示
4. **完成**：文件生成在指定位置

## 🔍 故障排除

**Q: 为什么没有检测到我选中的目录？**
A: 确保在项目视图中**单击**目录，而不是文件。如果选中的是文件，系统会使用文件所在的父目录。

**Q: 生成的文件在哪里？**
A: 生成成功后会显示完整路径，如："成功生成Java文件: UserService.java 路径: /project/src/main/java/com/example/UserService.java"

**Q: 可以生成到项目外的目录吗？**
A: 通过手动选择目录，理论上可以，但建议保持在项目内以便IDE管理。

## 🎉 新功能优势

- ✅ **智能检测**：自动识别选中目录
- ✅ **灵活选择**：支持手动选择任意目录  
- ✅ **包结构**：智能处理包目录创建
- ✅ **批量支持**：批量生成时保持一致性
- ✅ **路径反馈**：生成后显示完整文件路径 